# Better Auth Configuration
# Generate a secure random string for production: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
BETTER_AUTH_SECRET=your-secure-secret-key-here

# Better Auth URL - Update this for production deployment
BETTER_AUTH_URL=http://localhost:3000

# Backend API Configuration - Update this to your Cloudflare Workers URL for production
BACKEND_URL=http://localhost:8787

# Development Environment
NODE_ENV=development
